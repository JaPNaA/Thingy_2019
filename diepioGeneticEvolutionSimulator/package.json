{
  "name": "diepiogeneticevolutionsimulator",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && webpack --config webpack.prod.config.js",
    "start": "x-terminal-emulator --working-directory=. -e tsc -w; x-terminal-emulator --working-directory=. -e webpack --config webpack.config.js; hs ."
  },
  "author": "JaPNaA",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "open-simplex-noise": "^1.6.0"
  }
}
